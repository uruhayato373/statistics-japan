export interface AppsPropsType {
  params: {
    pageId?: string
    prefCode?: string
  }
}

export interface RouterPropsType {
  fieldId: string
  menuId: string
  kindId: string
  pageId?: string
  cardId?: string
  prefCode?: string
}
